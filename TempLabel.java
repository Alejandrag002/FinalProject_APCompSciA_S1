import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class tempLabel here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class TempLabel extends Actor
{
    
    TempLabel(){
            setImage(new GreenfootImage("yourmom", 24, Color.GREEN, Color.BLACK));
            //GreenfootImage text = new GreenfootImage("your mom", 30, Color.GREEN, Color.BLACK); 
            //text.drawString(text, 2, 20);
            //setImage(text);
    }
    /**
     * Act - do whatever the tempLabel wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
}
